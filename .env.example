# Optional: set these if deploying under a subpath or custom domain
# For Dokploy: Set these in the Environment tab BEFORE first deploy (build-time variables)

# Your public-facing URL (with https://)
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# Base path for the app (use / for root, or /resume for subpath deployment)
NUXT_PUBLIC_BASE_URL=/

# Site name shown in browser tab and PWA manifest
NUXT_PUBLIC_SITE_NAME=Markdown Resume

# Optional: Google Fonts API key for custom font loading in the editor
# NUXT_PUBLIC_GOOGLE_FONTS_KEY=your-google-fonts-api-key
